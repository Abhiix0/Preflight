/**
 * Repositories page — stub.
 *
 * TODO (repositories feature phase): Build the full repositories UI here:
 *   - List connected repositories (GET /repositories)
 *   - Connect repository flow (POST /repositories)
 *   - Per-repo scan history and scores
 *   - Connect Repository button → modal/sheet with GitHub repo picker
 *
 * This page is intentionally empty until the Repositories feature phase.
 */
export default function RepositoriesPage() {
  return null;
}
